<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stockfinder</title>
    <link rel="preconnect" href="https://fonts.googleapis.com"> 
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> 
    <link href="https://fonts.googleapis.com/css2?family=Teko:wght@300&display=swap" rel="stylesheet">
    <!-- CAN INCLUDE BOOTSTRAP IN HERE TO STANDARDISE LAYOUT -->
    <!-- CSS only -->
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/../css/main.css">
</head>
<body>
    <header class="fixed-top">
        <div id="logoContainer">
            <!-- <p><b>LOGO</b></p> -->
            <a href="home"><img id="navLogo" src="/images/logo.png"></a>
        </div>

        <div id="navBar">
            <nav class="left">
                <a href="search"><-- Return to Search</a>
                <%if (currentUser) {%>
                    | <a href="#">Secert Nav 1</a>
                <%}%> |

                <a href="home">New Search</a> 
                <%if (currentUser) {%>
                    | <a href="#">Secert Nav 2</a>
                <%}%>
            </nav>
            <nav class="center">
                <p>Tagline</p>
            </nav>
            <nav class="right">
                <%if (currentUser) {%>
                    <p>Hello <%=currentUser.username%> |
                    <a href="profile?id=<%=currentUser.id%>">Profile</a></p>
                <%}%>
            </nav>
        </div> 
        <div id="authenticationControls">
            <nav class=>
                <%if (currentUser) {%>
                    <a href="logout">Logout</a></p>
                <%} else {%>
                    <a href="signup">Create an Account</a> |
                    <a href="signin">Sign In</a>
                </p>
                <%}%>
            </nav>
        </div> 
    </header>
    <br>
    <br>
    <div id="alertmessages">
        <% if(alerts.validationErrors) {%>
            <% alerts.validationErrors.forEach(msg=>{%>
                <div class="alert alert-danger">
                    <%=msg.msg%>
                </div>
            <%})%>
        <%}%>
        <% if(alerts.success) {%>
            <% alerts.success.forEach(msg=>{%>
                <div class="alert alert-success"><%=msg%></div>
            <%})%>
        <%}%>
        <% if(alerts.error) {%>
            <% alerts.error.forEach(msg=>{%>
                <div class="alert alert-danger"><%=msg%></div>
            <%})%>
        <%}%>
    </div>
    <%- body %>
    <br><br>
    <footer class="fixed-bottom">
        <hr>
        <p class="left">Disclaimer: This site helps you discover stocks on the market. Search results do not constitute financial advice. You are responsible for your own investment decisions.</p>
        <p class="center">Made by Camiel: <a href="www.camiel.co.nz">Website</a> | <a href="#">LinkedIn</a> | <a href="#">Github</a></p>
        <p class="right">Bored of stocks and feel like <a href="#">blobbing</a>?</p>
        <script
            src="https://code.jquery.com/jquery-3.6.0.min.js"
            integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
            crossorigin="anonymous">
        </script>
        <script src="../js/main.js"></script>
    </footer>
</body>
</html>